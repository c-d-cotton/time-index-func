# Import all names from the time_index_func module directly into the
# top-level package namespace. This pattern should typically not be used
# to avoid cluttering the namespace, but this case of a package with a
# single module is reason for an exception for the sake of convenience.
from .time_index_func import *